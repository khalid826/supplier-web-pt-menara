/**
 * client.ts
 *
 * TypeScript interface for client data structure.
 * Used in: components/clients, data/clients.json, and anywhere client typing is needed.
 */
export interface Client {
  id: string;
  name: string;
  logo: string;
} 