/**
 * contact.ts
 *
 * TypeScript interface for contact information structure.
 * Used in: components/contact, data/contact.json, and anywhere contact typing is needed.
 */
export interface ContactInfo {
  company: string;
  address: string;
  email: string;
  phone: string;
} 